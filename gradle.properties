# Project-wide Gradle settings.
# https://docs.gradle.org/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8

# AndroidX package structure. Use the flag to opt-in to the new package structure.
android.useAndroidX=true

# Enables namespacing of each library's R class so that its R class includes only
# the resources declared in the library itself and none from the library's dependencies.
android.nonTransitiveRClass=true

# Kotlin code style for this project: "official" or "obsolete".
kotlin.code.style=official

# Enables incremental annotation processing.
kapt.incremental.apt=true

# Use Gradle configuration cache for faster builds.
org.gradle.configuration-cache=false

# Build performance: parallel builds.
org.gradle.parallel=true

# Caching.
org.gradle.caching=true
